# Adapter SDK

This Module owns workspace-root discovery, Adapter capability normalization, registry lookup, and registered projector loading. It exposes one host-neutral seam while each Adapter keeps its projector implementation, host facts, validation, and lifecycle.
