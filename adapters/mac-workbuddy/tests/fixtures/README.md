# Test-only fixtures

This directory contains neutral, synthetic contract inputs for the WorkBuddy
Adapter. They are incomplete without media generated at test runtime, are not
installable theme packages, are never scanned by the runtime, and are excluded
from every release archive.
