{
  "name": "@cc-theme/win-workbuddy",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Paused-by-user Windows WorkBuddy Adapter baseline preserved for reference",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "for file in src/*.mjs scripts/*.mjs tests/*.mjs; do node --check \"$file\"; done"
  },
  "engines": {
    "node": ">=20"
  }
}
