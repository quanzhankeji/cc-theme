# Test-only media

This directory is outside every Adapter release allowlist. Its files are
neutral, synthetic contract inputs and are not installable themes, presets, or
catalog content.

`neutral-v2-atlas.webp` is a 188-byte fully transparent RGBA WebP used only to
exercise deterministic Codex v2 atlas validation. It contains no branded
artwork.
